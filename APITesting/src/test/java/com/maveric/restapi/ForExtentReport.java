package com.maveric.restapi;

import com.relevantcodes.extentreports.ExtentReports;

public class ForExtentReport {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
/*
		ExtentReports report=new ExtentReports("C:\\\\Users\\\\Lenovo\\\\Documents\\\\docs\\\\extent.html");
		ExtentHtmlReporter htmlReporter = new ExtentHtmlReporter("C:\\Users\\Lenovo\\Documents\\docs\\extent.html");
	    
        // create ExtentReports and attach reporter(s)
        ExtentReports extent = new ExtentReports();
        extent.attachReporter(htmlReporter);

        // creates a toggle for the given test, adds all log events under it    
        ExtentTest test = extent.createTest("MyFirstTest", "Sample description");

        // log(Status, details)
        test.log(Status.INFO, "This step shows usage of log(status, details)");

        // info(details)
        test.info("This step shows usage of info(details)");
    
        String text = "extent";
        Markup m = MarkupHelper.createLabel(text, ExtentColor.BLUE);
        test.log(Status.PASS, m);
        
        
        String[][] data = {
        	    { "<b font color='red'>Actual</b>", "<b>Expected</b>", "<b>Status</b>" },
        	    { "Content.1.1", "Content.2.1", "Content.3.1" },
        	    { "Content.1.2", "Content.2.2", "Content.3.2" },
        	    { "Content.1.3", "Content.2.3", "Content.3.3" },
        	    { "Content.1.4", "Content.2.4", "Content.3.4" }
        	};
        	Markup m1 = MarkupHelper.createTable(data);
        	



        	test.log(Status.PASS, m1);
        

        
        // calling flush writes everything to the log file
        extent.flush();
	*/
	}

}
